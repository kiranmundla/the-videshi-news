/* CategoryPills — deprecated in favor of the persistent nav bar in Masthead.
   Kept as a no-op export so existing page imports don't break. */
export default function CategoryPills() {
  return null;
}
